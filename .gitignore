# Gradle and IDE
.gradle/
build/
app/build/
local.properties
*.iml
.idea/
.DS_Store

# Signing material (the CI job creates and force-adds it)
keystore.properties

# Downloaded at build time by tools/fetch_face_model.sh and tools/fetch_pose_model.sh
app/src/main/assets/models/

# Downloaded at build time by tools/fetch_vrm_model.sh (the 3D character)
app/src/main/assets/three/

# Build outputs
out/
ci-out/
